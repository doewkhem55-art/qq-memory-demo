# AGENTS

后续 coding agent 进入本项目时，请先阅读并遵守以下规则。

1. 每次开发前必须先阅读 `PROJECT_CONTEXT.md`、`AGENTS.md` 和 `NEXT_TASKS.md`。
2. 不允许重新初始化项目。
3. 不允许重做首页首屏。
4. 不允许删除已有页面。
5. 不允许删除 `mockData.js` 和 `aiMemoryService.js`。
6. 修改前必须先理解当前页面结构、demo state、`aiMemoryService.js` 和 `mockData.js`。
7. 每次任务完成后必须运行 `npm run build`。
8. 优先保持产品链路完整，而不是只做局部视觉。
9. 当前项目核心目标是腾讯产品大赛高保真 Demo，不是普通练习网页。
10. 页面风格必须保持深色沉浸式、蓝紫光感、玻璃拟态、点阵标题、数字记忆感。
11. UI 中应尽量避免直接暴露 Mock AI 字样，工程说明应放在代码注释或项目文档中。
