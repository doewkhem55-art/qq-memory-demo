# PROJECT_CONTEXT

## 项目名称

QQ 时光回廊

## 最终目标

高标准完成腾讯产品大赛网页 Demo，做成精美、可用、可演示、可部署的高保真网页，具备完整产品体验流程、未来 AI 接口预留、持续归档闭环和比赛展示能力。

## 技术栈

Vite + React + Tailwind CSS + lucide-react。

## 当前阶段状态

- 第一阶段：首页高保真首屏，基本完成。
- 第二阶段：完整 Demo 主链路跑通，基本完成。
- 2.5 阶段：视觉风格统一与精修，基本完成。
- 第三阶段 3A：AI 分类可信度增强，已完成。
- 第三阶段 3B：相册照片系统、真实感素材与大图预览，已完成 checkpoint。
- 下一阶段：第三阶段 3C，全站视觉表达精修。

## 当前已完成页面

- Home 首页
- ImportMemory 记忆导入页
- Analyzing AI 分析页
- MemoryClusters 记忆分类结果页
- MemoryDetail 记忆详情页
- MemoryPage 回忆页生成页

## 当前已完成能力

- 首页深色沉浸式时光穹顶视觉
- 首页点阵风格一级标题
- 首页产品亮点区与能力弹窗
- 首页体验流程区与隐私保护区
- 选择记忆来源
- 选择分类方式
- 输入自定义 AI 分类指令
- 本地图片上传与浏览器本地预览
- Mock AI 分类流程
- 上传图片进入详情页照片墙和回忆页精选照片
- 记忆分类结果页
- 记忆详情页
- 回忆页生成
- 从今天开始持续归档
- 新归档相册可回到 MemoryClusters
- 新归档相册可编辑和删除
- customPrompt 能明显影响 AI 分类结果
- 结果页展示用户输入的 AI 分类指令
- 每个 memoryCluster 增加 classificationReasons
- MemoryClusters 增加“查看 AI 分类依据”面板
- 修复了“查看 AI 分类依据”入口重复的问题
- UI 中已弱化 Mock AI 工程化表达
- 保留未来多模态 AI 接口预留说明
- aiMemoryService.js 作为未来 AI 接口服务层
- 3B-1：相册照片展示基础建设已完成
- 3B-2：真实感 Demo 相册素材接入已完成
- 3B-2b：PhotoLightbox 大图预览能力已完成
- 用户上传图片、Demo 图片均支持大图预览
- MemoryDetail 和 MemoryPage 已接入照片大图查看
- MemoryClusters 相册封面图和缩略图已支持大图查看
- 真实感照片优先于 SVG fallback
- 上传图片仍保持最高优先级
- 二次上传追加、多图分配、照片数量自洽逻辑保持正常

## 当前必须保留的要求

- 不要重做首页首屏
- 不要重新初始化项目
- 不要删除 aiMemoryService.js
- 不要删除 mockData.js
- 不要破坏完整 Demo 主链路
- 不要破坏上传图片进入详情页和回忆页的能力
- 不要破坏自定义 AI 分类能力
- 不要破坏持续归档功能
- 不要把 Mock 数据散落在页面组件中
- 保持深色沉浸式、玻璃拟态、蓝紫光感、点阵标题、数字记忆感、电影感
- 所有修改都要服务于最终腾讯产品大赛 Demo

## 当前还存在的问题

- 默认自动分类相册也需要统一支持重命名、编辑描述、设置可见范围、隐藏
- 3C 需要在不破坏照片系统的前提下继续做全站视觉表达精修
- 后续需要部署公网链接并录制演示视频

## 当前运行命令

```bash
npm run dev
npm run build
```

## 3B checkpoint：照片素材与大图预览完成

当前第三阶段 3B 已完成 checkpoint。

已完成内容：
- 3B-1：相册照片展示基础建设已完成。
- 3B-2：真实感 Demo 相册素材接入已完成。
- 3B-2b：`PhotoLightbox` 大图预览能力已完成。
- 用户上传图片、Demo 图片均支持大图预览。
- `MemoryDetail` 和 `MemoryPage` 已接入照片大图查看。
- `MemoryClusters` 相册封面图和缩略图已支持大图查看。
- 真实感照片优先于 SVG fallback。
- 上传图片仍保持最高优先级。
- 二次上传追加、多图分配、照片数量自洽逻辑保持正常。

继续开发注意：
- 下一阶段进入 3C：全站视觉表达精修。
- 3C 不应破坏 3B 已完成的照片系统、真实感素材、上传图片优先级和 Lightbox。
- 暂时不要接真实后端，不要重做首页，不要进入相册管理统一阶段。

## 3B 最终保存记录：curated 真实照片与相册封面稳定

当前第三阶段 3B 已完成最终保存，可以进入下一阶段规划。

已完成内容：
- curated 真实感照片素材已接入 `public/demo-photos/curated`，并通过 `photoAssets.js` 作为主展示素材使用。
- 相册首页 `MemoryClusters` 的主封面已能稳定显示真实照片，不再只显示深色空底、渐变占位或只有缩略图有图。
- `MemoryDetail` / `MemoryPage` 的照片墙、精选照片正常显示真实感照片。
- `PhotoLightbox` 已支持点击放大，用户上传图片和 Demo 图片均可预览。
- 用户上传图片仍然保持最高优先级，优先于 curated 真实照片、Demo fallback 和 SVG fallback。
- `recent` 仍作为用户后续持续归档入口，不强行接入 curated 默认素材。
- 照片数量、封面显示、详情页和回忆页展示逻辑目前基本稳定。
- 封面图片已做语义匹配和错峰展示，避免相邻相册大量重复主封面。
- 小统计文案已改为稳定格式，避免“检索 28 /...”之类截断。

下一阶段准备进入：
- 第三阶段 3C：相册管理产品化。
- 重点是让默认相册和持续归档相册具备更完整的管理能力，同时不破坏 3B 已完成的照片系统、Lightbox、上传图片优先级和主链路。

## 3B-1 阶段性状态记录（历史）

第三阶段 3B-1：相册内容真实感补齐基础建设已完成，后续 3B-2 和 3B-2b 已在上方 checkpoint 中记录。

已完成内容：
- 已建立并优化 `photoAssets` 照片资产体系，默认记忆包开始具备统一照片资产入口。
- 已建立 `public/demo-photos` 目录及相关说明，用于放置 Demo 图片素材和本地安全占位素材。
- 已新增或优化 `PhotoCard` / `PhotoGrid` 等照片展示组件。
- `MemoryClusters`、`MemoryDetail`、`MemoryPage` 已开始统一读取照片资产。
- 上传本地图片已经能够参与分类结果展示，并优先进入对应相册封面、详情页照片墙和回忆页精选照片。
- 已修复部分照片卡片文字重叠、标签挤压、时间信息错位问题。
- 已修复部分相册封面只显示文件名、不显示上传图片的问题。

仍需注意：
- 后续 3C 精修时，应继续确认不同分类方式和持续归档链路下图片都稳定显示。
- 当前 demo-photos 已接入真实感 Demo 图片，SVG 仅作为 fallback 保留。
## 3B-1 checkpoint：相册照片展示基础建设完成

当前第三阶段 3B-1：相册照片展示基础建设已完成 checkpoint；3B-2 真实感素材和 3B-2b 大图预览也已完成。

已完成内容：
- `photoAssets` 照片资产体系已建立，默认记忆包具备统一照片资产入口。
- `public/demo-photos` 目录已建立，用于承载本地安全 Demo 图片素材与占位素材。
- `PhotoCard` / `PhotoGrid` 已用于统一照片展示，支持上传图片、Demo 素材和 fallback 占位。
- `MemoryClusters`、`MemoryDetail`、`MemoryPage` 已统一读取照片解析结果。
- 上传图片已能作为相册封面和照片墙内容显示。
- 二次上传已改为追加，不覆盖第一次上传。
- 多张上传图片已能按当前分类方式分配到多个记忆包，而不是全部集中到第一个相册。
- 照片数量已区分检索数量、当前预览数量和本地补全数量。
- 已修复封面文字重叠、标签挤压、时间错位等布局问题。
- 已修复上传图片进入 state 但封面不显示图片本身的问题。

继续开发注意：
- 下一阶段进入 3C 前，应先确认 3B 照片系统、Lightbox、上传图片和持续归档链路都稳定显示。
- 暂时不要进入相册管理统一，不要接真实后端，不要重做首页。
## 3C checkpoint：相册管理产品化完成

当前第三阶段 3C 已完成并保存为可继续开发 checkpoint。

已完成内容：
- 所有记忆包统一支持重命名，覆盖默认自动分类、自定义 AI 分类、持续归档和上传图片参与生成的相册包。
- 记忆包描述可在统一管理面板中编辑，MemoryClusters、MemoryDetail、MemoryPage 同步读取同一份 cluster state。
- 支持删除 / 隐藏任意记忆包，只从当前 Demo state 中移除，不删除 public/demo-photos 素材；详情页或回忆页删除后自动返回 MemoryClusters。
- 新增统一 `MemoryManagePanel`，包含封面预览、标题、描述、可见性和删除入口。
- MemoryDetail 与 MemoryPage 右上角新增“管理”入口，MemoryClusters 卡片新增轻量管理入口。
- 支持轻量可见性状态：仅自己可见、可分享给好友、可同步到 QQ 空间，默认按仅自己可见表达隐私控制。
- 详情页 / 回忆页照片墙支持设为封面、从记忆包移除；封面同步影响列表卡片、详情页顶部封面和回忆页封面。
- 移除照片只影响当前记忆包和当前 Demo state，不删除原始素材；移除当前封面后自动回退到剩余第一张可显示照片。
- PhotoLightbox、curated 图片、上传图片优先级、recent 持续归档入口和二次上传追加链路保持可用。
- `npm run build` 已通过。

后续继续开发注意：
- 不要回退 3C 的统一管理状态函数与 `MemoryManagePanel`。
- 后续如做视觉精修，应保留照片级操作、封面覆盖顺序、removedPhotoIds 过滤和可见性状态。
## 3C polish checkpoint：管理收尾与稳定性验证

当前 3C 收尾优化已完成。

已完成：
- 相册卡片管理入口已轻量化，卡片只保留一个 hover 出现的“管理”入口，编辑标题、描述、可见性与删除都收敛到统一面板。
- 删除确认从浏览器 confirm 优化为产品化二次确认弹窗，明确说明只移除当前整理结果，不删除原始照片或本地素材。
- 管理面板加入“修改已同步”轻反馈，并补充“可在照片墙中选择其他照片设为封面。”提示。
- 照片统计文案统一为稳定的“X 张照片”，避免小卡片内出现“检索 / 预览”组合造成截断。
- 修正移除上传照片时的计数边界，避免持续归档上传图被重复计入 sourcePhotoCount。
- `npm run build` 已通过，PhotoLightbox、上传图优先级、curated 图片和 recent 持续归档链路保持稳定。

## 4A checkpoint：Hero 动态主视觉与回忆页文案精修

当前第四阶段 4A 已完成并保存为可继续开发 checkpoint。

已完成：
- 首页动态主视觉已完成，保留深蓝黑底、蓝紫光感、时光轨道、星点、光流和沉浸式 Hero 氛围。
- 首页已补充“未来持续记录 / QQ 空间成为个人生活记录器”的产品表达，覆盖 Hero 副文案、产品亮点、体验流程和隐私保护区域。
- 回忆页“AI 生成短文”已支持根据不同相册展示不同内容，覆盖高中毕业季、大学开学、班级聚餐、家人旅行、军训、朋友关系、自定义分类和 recent 持续归档等场景。
- 回忆页短文支持用户编辑，用户保存后写入当前记忆包 state，并在 MemoryPage 中优先显示用户修改后的文案。
- 3B/3C 功能保持稳定：curated 图片、用户上传图片优先级、PhotoLightbox、相册管理、设置封面、移除照片、可见性、删除相册和 recent 持续归档均保持可用。
- `npm run build` 已通过。

后续继续开发注意：
- 不要再大改首页 Hero，除非进入明确的新阶段。
- 不要破坏 4A 的未来持续记录表达和回忆页短文编辑能力。
- 继续保持 3B/3C 的照片系统、管理状态、recent 持续归档和主链路稳定。
