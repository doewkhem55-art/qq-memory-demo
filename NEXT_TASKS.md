# 第三阶段 3C：相册管理产品化

当前第三阶段 3B 已完成 checkpoint，可以进入 3C。

## 3B 已完成状态

- curated 真实感照片素材已接入。
- 相册首页主封面已能显示真实照片。
- `MemoryDetail` / `MemoryPage` 照片墙正常显示。
- `PhotoLightbox` 可点击放大，支持用户上传图片和 Demo 图片。
- 用户上传图片仍然优先显示。
- `recent` 仍作为用户后续持续归档入口，不强行填充默认 curated 素材。
- 照片数量、封面、详情页展示逻辑基本稳定。

## 3C 下一阶段重点

1. 默认自动分类相册支持统一管理能力。
2. 持续归档新增相册与默认相册在管理体验上保持一致。
3. 支持相册重命名、编辑描述、设置可见范围、隐藏或删除。
4. 管理操作需要保持 Demo 主链路完整。
5. 管理状态要与 `MemoryClusters`、`MemoryDetail`、`MemoryPage` 展示保持一致。
6. 不接真实后端，继续使用当前 Demo state。

## 保持不变

- 不要重做首页。
- 不要破坏 Home -> ImportMemory -> Analyzing -> MemoryClusters -> MemoryDetail -> MemoryPage 主链路。
- 不要破坏 3B 已完成的照片资产体系。
- 不要破坏真实感照片优先于 SVG fallback 的逻辑。
- 不要破坏 `PhotoLightbox` 大图预览能力。
- 不要破坏上传图片最高优先级。
- 不要破坏二次上传追加、多图分配和照片数量自洽逻辑。
- 不要破坏 customPrompt 分类。
- 不要破坏 AI 分类依据面板。
- 不要破坏持续归档功能。
- 不要删除 `mockData.js`、`aiMemoryService.js`、`photoAssets.js`。
## 3C 完成记录：相册管理产品化

当前 3C 已完成，可进入下一阶段。

已完成：
- 统一管理能力覆盖默认相册、自定义 AI 分类相册、持续归档相册和上传图片参与的相册。
- 支持重命名、编辑描述、删除 / 隐藏、设置封面、移除照片和可见性切换。
- MemoryClusters、MemoryDetail、MemoryPage 使用同一份 cluster state，同步显示标题、描述、封面、数量和可见性。
- 删除 / 隐藏只影响当前 Demo state，不删除素材文件。
- 照片移除只影响当前记忆包；封面被移除后回退到剩余第一张。
- PhotoLightbox、curated 图片、上传图片优先级、recent 持续归档入口保持稳定。
- `npm run build` 已通过。

下一阶段建议：
1. 进入第四阶段前先人工走查主链路：导入 -> 分析 -> 详情 -> 管理 -> 回忆页 -> 持续归档。
2. 可继续做全站视觉表达精修，但不要重做首页首屏，不要破坏 3B/3C 的照片和管理链路。
3. 后续可补充演示脚本、部署公网链接和参赛讲解材料。
## 3C 收尾优化完成

当前 3C polish 已完成，可作为新的 checkpoint。

已完成：
- 管理入口轻量化：MemoryClusters 卡片仅保留一个“管理”入口，避免卡片呈现后台工具栏感。
- 删除确认文案与交互优化：使用产品化确认弹窗，说明不会删除原始照片或本地素材。
- 管理弹窗体验微调：保留产品化说明，加入封面提示和“修改已同步”反馈。
- 统计信息稳定化：照片数量统一显示为“X 张照片”，不再显示拥挤的“检索 / 预览”组合。
- 已验证 `npm run build` 通过，PhotoLightbox、curated 图片、上传图优先级和 recent 持续归档入口保持可用。

下一步建议：
1. 可以进入下一阶段前的完整演示走查。
2. 后续不要新增相册管理大功能，除非进入新的明确阶段。
3. 保持 3B 图片资产与 3C 管理状态链路稳定。

## 4A 完成记录：Hero 与回忆页文案精修

当前 4A 已完成，可作为进入下一阶段前的 checkpoint。

已完成：
- 首页动态主视觉已完成，首屏保持大尺度时光场、蓝紫光环、星点和轻动态氛围。
- 首页已补充“未来持续记录 / QQ 空间成为个人生活记录器”的表达，旧回忆整理与从今天继续记录形成闭环。
- 回忆页 AI 生成短文已支持根据不同相册生成不同内容，不再共用同一句通用模板。
- 回忆页短文支持用户编辑，编辑后的 `memoryPageText` 会优先显示在 MemoryPage。
- 3B/3C 功能保持稳定：照片资产、PhotoLightbox、相册管理、封面、移除照片、可见性、删除相册、recent 持续归档和用户上传图片优先级均保留。
- `npm run build` 已通过。

下一阶段建议：
1. 可以进入下一阶段规划或完整演示材料准备。
2. 暂停新增功能前，优先保持当前高保真 Demo 的稳定性。
3. 后续如继续开发，不要重做首页首屏，不要破坏 4A Hero、未来持续记录表达和回忆页短文编辑能力。

## 4B 完成记录：全链路演示体验检查

当前 4B 第一轮已完成，可作为后续演示与部署基础版本。

已完成：
- 4B 全链路体验检查已完成。
- 首页 -> 导入 -> AI 分析 -> 分类结果 -> 详情页 -> 回忆页 -> 保存 / 分享 / 继续归档的核心演示路径已跑通。
- 回忆页最终动作区已补齐产品闭环：用户能清楚知道回忆页已生成完成，可以保存为仅自己可见、生成 QQ 空间分享草稿，也可以继续归档近期照片。
- recent 持续归档入口保持复用已有流程，不新增复杂功能。
- 临时测试素材目录已清理，不纳入提交。
- `npm run build` 已通过。

下一阶段建议：
1. 可以进入部署、公网预览链接、演示脚本和参赛讲解材料准备。
2. 后续如继续打磨，优先修小的演示断点，不要新增大功能或重构业务逻辑。
3. 继续保持 3B/3C/4A/4B 的照片、管理、Hero、回忆页收尾闭环和主链路稳定。

## 4C 完成记录：评委演示路径与讲述链路打磨

当前 4C 已完成，可作为后续部署与演示脚本准备的 checkpoint。

已完成：
- 导入页、分类结果页、详情页、回忆页的关键提示已从讲解稿式文案调整为更自然的产品内智能提示。
- 导入页明确 AI 会结合相册、说说、评论和好友互动整理记忆，本地照片补全为可选入口。
- 分类结果页强化“记忆包已结合照片时间、说说评论、共同出现人物和好友互动生成”，并保留“查看 AI 分类依据”和“进入这段记忆”的清晰路径。
- 详情页明确 AI 已将照片、时间线、说说评论和好友互动串联为一段记忆，并提示点击照片查看大图。
- 回忆页保留“先保存为仅自己可见、确认后分享、继续归档近期照片”的收尾闭环。
- 正式演示路径已确认：首页 -> 导入 -> AI 分析 -> 分类结果 -> 详情 -> 回忆页 -> 保存 / 分享 / 继续归档。
- `npm run build` 已通过。

下一阶段建议：
1. 进入 5A：部署与线上发布，准备公网预览链接。
2. 同步整理正式演示脚本和讲解材料，围绕“旧回忆整理 + 未来持续记录”主线展开。
3. 继续避免新增复杂功能、大改首页 Hero 或重构 demoState，优先保证高保真 Demo 稳定。

## 5A 完成记录：部署与线上发布

当前 5A 已完成，项目已经具备可提交、可分享的线上 Demo 链接。

已完成：
- 项目已推送到 GitHub。
- GitHub 仓库地址：https://github.com/doewkhem55-art/qq-memory-demo.git
- Netlify 已从 GitHub `main` 分支成功部署。
- 线上 Demo 链接：https://mellifluous-chaja-2ca41e.netlify.app
- 部署方式：Branch 为 `main`，Build command 为 `npm run build`，Publish directory 为 `dist`。
- 线上检查通过：首页正常；动态 Hero 正常；开始整理链路正常；相册封面正常；详情页照片墙正常；图片放大正常；回忆页生成正常；保存 / 分享 / 继续归档反馈正常。

部署注意事项：
- 内置 curated Demo 图片在线上可正常访问。
- 用户本地上传图片仍然是浏览器本地预览，不是云端持久化。
- 比赛演示时应优先使用内置 Demo 路径，不依赖刷新后保留本地上传图片。

下一阶段建议：
1. 进入 5B：最终演示材料准备。
2. 准备正式演示脚本、提交说明、讲解视频录制结构和评委观看路径。
3. 继续保持当前线上 Demo 稳定，不新增复杂功能，不改页面视觉，不重构主链路。
